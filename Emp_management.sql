Create database Emp_management;
use Emp_management;
create table Emp_management(
     id int primary key,
     Name varchar(20),
     Age int,
     salary decimal(10,2)
);
select *from Emp_management;